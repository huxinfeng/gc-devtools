import { defineConfig } from 'dumi';

export default defineConfig({
  base: '/gc-devtools/',
  outputPath: 'docs-dist',
  publicPath: '/gc-devtools/',
  themeConfig: {
    name: 'gc-devtools',
  },
});
